# Проверка инициативы

**Изменение:** `<change-name>`

<!-- SCENARIO_VERIFICATION_CONTRACT_V1_START -->
## Краткий вывод агента

- **Готовность к ручной проверке:** `READY` / `BLOCKED`
- **Проверено автоматически:** `<типы проверок, количество и общий итог>`
- **Проверить человеку:** `<одно-два предложения>`
- **Риски:** `<кратко или «нет»>`

<!-- Это выжимка, не вставляй команды, логи, Git/API-историю, timestamps и рассуждения. -->

## Объект проверки

- **Кандидат:** `<какая версия, сборка или среда проверяется>`
- **Среда и данные:** `<что подготовить проверяющему>`

<!-- Опиши кандидата человеческим языком. Не указывай Repository ID, checkout, пути, файлы, symbols, revisions, хеши и URL. -->

## Сценарии ручной проверки

| Сценарий | Действия проверяющего | Ожидаемый результат | Статус агента | Решение человека |
| --- | --- | --- | --- | --- |
| `<Scenario ID или Acceptance case>` | `<предусловия и шаги>` | `<наблюдаемый результат>` | `PENDING` / `PASS` / `FAIL` / `N/A` | `PENDING` / `PASS` / `FAIL` / `N/A` |

<!-- Учти все Scenario. Для интерфейса, API и базы данных дай воспроизводимые действия. При отсутствии Delta Specs пометь кейсы Proposal как Acceptance case. -->

## Выполненные проверки

| Тип проверки | Выполнено | Результат |
| --- | --- | --- |
| `<unit / integration / e2e / валидация / другое>` | `<количество>` | `PASS` / `FAIL` / `N/A` / `PENDING` |

<!-- Указывай только типы выполненных проверок, их количество и итоговый статус. Не добавляй команды, имена и пути тестов, ссылки на отчёты или Code Repositories, сырые логи и диагностику окружения. -->
<!-- SCENARIO_VERIFICATION_CONTRACT_V1_END -->

## Открытые вопросы

- `<неподтверждённые Scenario и решения команд; при отсутствии — «нет»>`

## Заключение

`<кратко: что подтверждено и что осталось проверить>`
