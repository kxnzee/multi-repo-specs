# Проверка инициативы

**Изменение:** `<change-name>`

<!-- SCENARIO_VERIFICATION_CONTRACT_V1_START -->
## Краткий вывод агента

- **Готовность к ручной проверке:** `READY` / `BLOCKED`
- **Проверено автоматически:** `<одно-два предложения>`
- **Проверить человеку:** `<одно-два предложения>`
- **Риски:** `<кратко или «нет»>`

<!-- Это выжимка, не вставляй команды, логи, Git/API-историю, timestamps и рассуждения. -->

## Объект проверки

- **Требования:** `<локальный путь к принятым Specs>`
- **Кандидат:** `<одна локальная ссылка на канонический реестр версий>`
- **Среда и данные:** `<какие checkout кандидата и данные подготовить>`

<!-- Кандидата разрешает канонический реестр: не копируй revisions, хеши и PR/CI URL в Verify. -->

## Сценарии ручной проверки

| Сценарий | Действия проверяющего | Ожидаемый результат | Подтверждение агента | Решение человека |
| --- | --- | --- | --- | --- |
| `<Scenario ID или Acceptance case>` | `<предусловия и шаги>` | `<наблюдаемый результат>` | `<статус и локальное evidence>` | `PENDING` / `PASS` / `FAIL` / `N/A` |

<!-- Учти все Scenario. Для интерфейса, API и базы данных дай воспроизводимые действия. При отсутствии Delta Specs пометь кейсы Proposal как Acceptance case. -->

## Автоматические проверки

| Проверка | Результат | Evidence |
| --- | --- | --- |
| `openspec validate <change-name> --strict --no-interactive` | `PASS` / `FAIL` / `PENDING` | `<локальный путь или краткий итог>` |
| `<релевантная проверка>` | `PASS` / `FAIL` / `N/A` / `PENDING` | `<локальный путь или краткий итог>` |

<!-- Оставь только проверки результата; не вставляй сырые логи и диагностику окружения. -->
<!-- Не требуй GitHub/GitLab/Bitbucket API или MCP: provider evidence принадлежит каноническому реестру кандидата. -->
<!-- SCENARIO_VERIFICATION_CONTRACT_V1_END -->

## Открытые вопросы

- `<неподтверждённые Scenario и решения команд; при отсутствии — «нет»>`

## Заключение

`<кратко: что подтверждено и что осталось проверить>`
