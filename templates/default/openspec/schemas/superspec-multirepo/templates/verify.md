# Приёмка изменения

**Изменение:** `<change-name>`

<!-- SCENARIO_VERIFICATION_CONTRACT_V1_START -->
## Краткий вывод агента

- **Готовность к ручной проверке:** `READY` / `BLOCKED`
- **Проверено автоматически:** `<одно-два предложения>`
- **Проверить человеку:** `<одно-два предложения>`
- **Риски:** `<кратко или «нет»>`

<!-- Это выжимка, не вставляй команды, логи, Git/API-историю, timestamps и рассуждения. -->

## Объект проверки

- **Требования:** `<ссылка на принятые Specs>`
- **Кандидат:** `<одна ссылка на канонический реестр версий>`
- **Среда и данные:** `<где и с какими данными проверять>`

## Сценарии ручной проверки

| Сценарий | Действия проверяющего | Ожидаемый результат | Подтверждение агента | Решение человека |
| --- | --- | --- | --- | --- |
| `<Scenario ID или Acceptance case>` | `<предусловия и шаги>` | `<наблюдаемый результат>` | `<статус и ссылка>` | `PENDING` / `PASS` / `FAIL` / `N/A` |

<!-- Учти все Scenario. Для интерфейса, API и базы данных дай воспроизводимые действия. При отсутствии Delta Specs пометь кейсы Proposal как Acceptance case. -->

## Автоматические проверки

| Проверка | Результат | Evidence |
| --- | --- | --- |
| `openspec validate <change-name> --strict --no-interactive` | `PASS` / `FAIL` / `PENDING` | `<ссылка или краткий итог>` |
| `<релевантная проверка>` | `PASS` / `FAIL` / `N/A` / `PENDING` | `<ссылка>` |

<!-- Оставь только проверки результата; не вставляй сырые логи и диагностику окружения. -->
<!-- SCENARIO_VERIFICATION_CONTRACT_V1_END -->

<!-- FEATURE_ACCEPTANCE_CONTRACT_V1_START -->
## Решение о приёмке

- **Решение:** `PENDING` / `PASS` / `FAIL`
- **Комментарий:** `<краткое обоснование или ссылка>`

<!-- Агент готовит выжимку и проверочные кейсы, но решение о приёмке принимает человек. До явного решения сохраняй PENDING. -->
<!-- FEATURE_ACCEPTANCE_CONTRACT_V1_END -->

## Соблюдение процесса Superspec

- **Результат:** `PASS` / `PASS_WITH_WARNINGS` / `FAIL`
- **Кратко:** `<одно предложение>`
- **Evidence:** `<ссылки без логов>`

## Открытые вопросы

- `<только блокеры и решения человека; при отсутствии — «нет»>`
