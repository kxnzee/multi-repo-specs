#!/usr/bin/env bash
# Usage: task-brief PLAN_FILE TASK_NUMBER [OUTFILE] [--repo REPOSITORY_ID]
# Without Bash: node <skill>/scripts/task-context.cjs brief <same arguments>
set -euo pipefail
exec node "$(cd "$(dirname "$0")" && pwd)/task-context.cjs" brief "$@"
