## 1. Store: Planning-артефакты и валидация

Цель: store

- [ ] 1.1 Строгая OpenSpec-валидация всех артефактов Change (proposal, specs, design, tasks) без ошибок. Результат: `openspec validate --changes --store test-store` проходит. Зависимости: нет. Проверка: повторный запуск валидации.
- [ ] 1.2 Компонентная проверка: каждый requirement в Delta Spec `module-status-display` имеет хотя бы один сценарий, и каждый сценарий покрывает наблюдаемое поведение из Proposal. Результат: полное покрытие. Зависимости: 1.1. Проверка: ручная проверка соответствия.
- [ ] 1.3 Контрактная проверка: контракт статусов в Delta Spec совпадает с контрактом в Design (ровно три значения: пройден, в процессе, план). Результат: расхождений нет. Зависимости: 1.1. Проверка: сравнение текста.

## 2. page: Потребление и отображение статусов модулей

Цель: page

- [ ] 2.1 Реализовать capability `module-status-display`: получение данных о модулях и их статусах из артефактов курса, отображение статусов модулей с визуальным различием, обработка отсутствия данных. Результат: при наличии артефактов курса отображаются модули со статусами; при отсутствии — уместное пустое состояние. Зависимости: 1.1. Проверка: открыть страницу рядом с артефактами курса и убедиться, что статусы отображаются корректно; при отсутствии артефактов — отображается пустое состояние.

## 3. learning-method-framework: Верификация отсутствия изменений

Цель: learning-method-framework

- [ ] 3.1 Подтвердить, что формат артефактов курса и словарь статусов не изменены реализацией. Результат: diff ревизии `75e71ec` и текущего HEAD не содержит изменений в артефактах курса и контракте статусов. Зависимости: 2.1. Проверка: `git diff 75e71ec..HEAD` в репозитории `learning-method-framework`.

## 4. Composite verification: Межсистемная интеграция

Цель: composite-verification

- [ ] 4.1 Сквозная проверка: страница корректно отображает статусы модулей из артефактов курса, поставляемых фреймворком, и корректно обрабатывает отсутствие данных. Результат: все сценарии из Delta Spec `module-status-display` проходят. Зависимости: 2.1, 3.1. Проверка: последовательная проверка каждого сценария из spec.

## 5. Архивация

Цель: store

- [ ] 5.1 Условия Archive выполнены: реализация в `page` завершена, верификация отсутствия изменений в `learning-method-framework` пройдена, composite verification пройдена. Результат: Change готов к архивации. Зависимости: 4.1. Проверка: все предыдущие задачи отмечены.
